# ============================================================
# Wonder World Westgate — Environment Variables
# Copy to .env and fill in real values.
# NEVER commit .env to git.
# ============================================================

# ── PostgreSQL (local) ──────────────────────────────────────
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=wonderworld
PG_USER=wonderworld
PG_PASSWORD=your_strong_password_here

# ── Firebase (server-side Admin SDK) ────────────────────────
# From: Firebase Console → Project Settings → Service Accounts → Generate new private key
FIREBASE_PROJECT_ID=wonderworld-westgate
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@wonderworld-westgate.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ── Firebase (client-side — safe to expose in frontend) ─────
# From: Firebase Console → Project Settings → Your apps → Web app config
FIREBASE_API_KEY=AIzaSy...
FIREBASE_AUTH_DOMAIN=wonderworld-westgate.firebaseapp.com
FIREBASE_STORAGE_BUCKET=wonderworld-westgate.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789
FIREBASE_APP_ID=1:123456789:web:abc123

# ── Stripe ──────────────────────────────────────────────────
STRIPE_PUBLIC_KEY=pk_live_...
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Resend (email) ──────────────────────────────────────────
RESEND_API_KEY=re_...

# ── Twilio (SMS) ────────────────────────────────────────────
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_PHONE_NUMBER=+64...

# ── Google Places API (live rating) ─────────────────────────
# Enable in Google Cloud Console → Places API
# Place ID: find at https://developers.google.com/maps/documentation/javascript/examples/places-placeid-finder
GOOGLE_PLACES_API_KEY=AIzaSy...
GOOGLE_PLACE_ID=ChIJ...

# ── POLi Payments (NZ bank-account checkout) ────────────────
# Both required for the "Bank Account" payment option to appear at all —
# without them it stays hidden and the site behaves exactly as before.
# From: POLi/Merco merchant console (consoles.apac.paywithpoli.com)
POLI_MERCHANT_CODE=
POLI_AUTH_CODE=

# ── App ─────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
SITE_URL=https://wonderworldwestgate.co.nz
ENVIRONMENT=production

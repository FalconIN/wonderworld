// DISABLED — config is now served dynamically from /api/config (server/index.js).
// Hardcoding secrets into HTML files is no longer necessary or permitted.
// See firebase.js for the client-side fetch pattern.
console.log('inject-env.js is disabled — config is served from /api/config');
